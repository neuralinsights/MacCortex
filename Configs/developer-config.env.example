# MacCortex 开发者配置
# Phase 0.5
# 创建时间: 2026-01-20
#
# 此文件包含开发者环境变量
# 请勿提交到 Git（已添加到 .gitignore）

# Apple Developer Team ID
export APPLE_TEAM_ID="CSRKUK3CQV"

# Developer ID 签名身份（证书申请完成后填写）
# 格式: Developer ID Application: Your Name (CSRKUK3CQV)
# 查询命令: security find-identity -v -p codesigning | grep "Developer ID"
export DEVELOPER_ID="Developer ID Application: YOUR_NAME (CSRKUK3CQV)"

# notarytool Keychain Profile 名称
export KEYCHAIN_PROFILE="notarytool-profile"

# Apple ID（用于公证）
export APPLE_ID="your@email.com"

# 使用说明:
# 1. 申请 Developer ID 证书后，更新 DEVELOPER_ID 变量
# 2. 更新 APPLE_ID 为您的 Apple ID 邮箱
# 3. 在终端执行: source Configs/developer-config.env
# 4. 验证: echo $APPLE_TEAM_ID
